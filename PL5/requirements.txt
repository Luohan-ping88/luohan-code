# ============================================================
# PL5 排列五预测系统 - 统一依赖文件
# 合并自根 requirements.txt 与 config/requirements.txt
# ============================================================

# --- 核心数据科学 (Python 3.14 兼容版本) ---
numpy>=2.5.0
pandas>=3.0.0
scipy>=1.18.0
scikit-learn>=1.9.0

# --- 时序模型 ---
statsmodels>=0.14.6
hmmlearn>=0.3.3

# --- 树模型 ---
lightgbm>=4.7.0
xgboost>=3.3.0
catboost>=1.2.10

# --- 调度与监控 ---
schedule>=1.2.0
psutil>=7.2.0
watchdog>=3.0.0

# --- API 服务 ---
fastapi>=0.104.0
uvicorn>=0.24.0
PyJWT>=2.8.0

# --- 数据获取 ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# --- 配置 ---
PyYAML>=6.0
python-dotenv>=1.0.0

# --- 图数据库（知识图谱闭环，V10.7 新增）---
kuzu>=0.11.0

# --- 安全 ---
cryptography>=41.0.0

# --- 工具 ---
joblib>=1.3.0
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0

# --- LLM 集成（可选，生产管线非必需） ---
# openai>=1.3.0
# llama-cpp-python>=0.2.0

# --- 深度学习（用于 Mamba/iTransformer/RL 模块，Python 3.14 需使用 CPU 版） ---
# 安装方式: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.13.0  # 取消注释后使用上述 URL 安装（PyPI 版可能暂不支持 Python 3.14）
# torchvision/torchaudio 按需安装（未在核心管线中使用）

# --- 扩展模块（高级数理分析 / 向量检索 / LLM 编排） ---
pybind11>=2.11.0
# 注意: 以下包依赖 numba，目前不支持 Python 3.14（需 >=3.10,<3.14）
# 生产管线非必需，缺失时将自动降级为替代模型
# pymc>=5.0.0       # 贝叶斯 MCMC（可选，依赖 numba，暂不支持 Py3.14）
# arviz>=0.15.0      # 贝叶斯可视化（可选，同上）
# copulae>=0.7.0     # Copula 相关性模型（可选，依赖 numba，暂不支持 Py3.14）
faiss-cpu>=1.7.4
# chromadb>=0.4.24   # 向量数据库（可选，LLM RAG 专用，生产预测非必需）
# langchain>=0.1.0   # LLM 编排（可选，生产预测非必需）

# --- 测试 ---
pytest>=9.0.0
pytest-asyncio>=0.21.0
allure-pytest>=2.13.0

# --- 开发工具 ---
black>=23.0.0
flake8>=6.0.0
